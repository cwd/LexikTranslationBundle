{% extends layout %}

{% block content %}
    <div class="row">
        <div class="col-md-12">
            {% embed "#{Helper}:portlet.html.twig" with {'title': "Translation"|trans, 'icon': icon|default('fa-language') } %}
                {% block body %}
                {{ form(form, {'style': 'horizontal', 'col_size': 'xs', 'align_with_widget': true, 'attr': {'novalidate': 'novalidate'}}) }}
                {% endblock %}
        {% endembed %}
    </div>
</div>
{% endblock %}
